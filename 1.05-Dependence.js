// Always declare dependencies explicitly.

// Practice

// The global variable
var fixedValue = 4;

// Only change code below this line
let incrementer = (fixedValue) => {
  return fixedValue += 1

  // Only change code above this line
}